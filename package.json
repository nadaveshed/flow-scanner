{
  "name": "flow-scanner",
  "version": "1.0.0",
  "description": "Naive scanner for API Gateway/Lambda → DynamoDB flows with OWASP vulnerability filtering",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "scan": "node dist/index.js",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "bin": {
    "flow-scanner": "./dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
